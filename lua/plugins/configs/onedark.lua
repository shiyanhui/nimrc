require("onedark").setup {
    style = "dark",
    -- transparent = true,
    -- lualine = {
        -- transparent = true,
    -- },
    code_style = {
        comments = "none",
    },
}

require("onedark").load()
